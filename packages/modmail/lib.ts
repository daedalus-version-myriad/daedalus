import type { Guild, Message, MessageComponentInteraction } from "discord.js";

export async function handleTargetSelection(message: Message, guild: Guild, prompt?: MessageComponentInteraction, allowReturn: boolean = false) {}
