import Panel from "./Panel";

export default function XpTechIssueAlert() {
    return (
        <Panel>
            <p>
                <b>Note:</b> Due to technical issues, we are currently unable to draw the level-up information on top of the background. Instead, it will be
                shown in the footer of an embed for now.
            </p>
        </Panel>
    );
}
